# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target '20plus' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks

  use_frameworks!
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire', '~> 4.7'

  # Pods for 20plus

  target '20plusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '20plusUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
